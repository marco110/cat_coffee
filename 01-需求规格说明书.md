# 爱猫咖啡 · 扫码点餐系统 —— 需求规格说明书

版本：v1.0
适用范围：微信小程序（顾客端 + 店主端）、Web 管理后台（超级管理员端）

---

## 目录

1. [角色与权限](#一角色与权限)
2. [业务术语与核心概念](#二业务术语与核心概念)
3. [顾客端功能需求](#三顾客端功能需求)
4. [店主端功能需求](#四店主端功能需求小程序内)
5. [超级管理员后台功能需求](#五超级管理员后台功能需求web)
6. [核心业务流程与订单状态机](#六核心业务流程与订单状态机)
7. [价格与营销计算规则](#七价格与营销计算规则)
8. [页面清单](#八页面清单)
9. [视觉与交互设计规范](#九视觉与交互设计规范)
10. [非功能需求](#十非功能需求)
11. [需求边界（本期不做）](#十一需求边界本期不做)

---

## 一、角色与权限

### 1.1 角色定义

| 角色 | 代码 | 入口 | 登录方式 | 说明 |
| --- | --- | --- | --- | --- |
| 顾客 | `CUSTOMER` | 微信小程序 | 微信授权 + 手机号一键授权 | 到店消费的顾客 |
| 店主 | `OWNER` | 微信小程序（角色切换） | 手机号 + 密码 | 门店经营者，拥有本店全部权限 |
| 店员 | `STAFF` | 微信小程序（角色切换） | 手机号 + 密码 | 可选角色，权限由店主勾选（P2） |
| 超级管理员 | `SUPER_ADMIN` | Web 管理后台 | 账号 + 密码 + 图形验证码 | 平台运营者，负责开通门店与店主账号 |
| 运营人员 | `OPERATOR` | Web 管理后台 | 账号 + 密码 + 图形验证码 | 可选角色，仅查看看板（P2） |

> **关键约束**：店主账号**不能自行注册**，必须由超级管理员在后台创建，并绑定到具体门店。这是"店主由超级管理员进行配置"的落地方式。

### 1.2 权限矩阵

| 功能 | 顾客 | 店主 | 店员 | 超级管理员 |
| --- | :-: | :-: | :-: | :-: |
| 浏览菜单 | ✅ | ✅ | ✅ | ✅（跨门店） |
| 提交订单 | ✅ | ❌ | ❌ | ❌ |
| 查看本人订单 | ✅ | - | - | - |
| 查看本店全部订单 | ❌ | ✅ | ✅ | ✅（跨门店） |
| 接单 / 拒单 / 出餐 / 完成 | ❌ | ✅ | ✅ | ❌ |
| 改单（增删菜品、改数量） | ❌ | ✅ | ✅ | ❌ |
| 标记已收款 | ❌ | ✅ | ✅ | ❌ |
| 分类 / 菜品管理 | ❌ | ✅ | 可配置 | ❌ |
| 桌位与二维码管理 | ❌ | ✅ | 可配置 | ❌ |
| 优惠券创建与发放 | ❌ | ✅ | 可配置 | ❌ |
| 会员等级规则配置 | ❌ | ✅ | ❌ | ✅（模板级） |
| 会员积分调整 | ❌ | ✅ | 可配置 | ❌ |
| 店铺信息 / 营业状态 | ❌ | ✅ | ❌ | ✅ |
| 门店 CRUD | ❌ | ❌ | ❌ | ✅ |
| 店主账号 CRUD / 重置密码 | ❌ | ❌ | ❌ | ✅ |
| 平台级数据看板 | ❌ | ❌ | ❌ | ✅ |
| 全局配置 / 操作日志 | ❌ | ❌ | ❌ | ✅ |

---

## 二、业务术语与核心概念

| 术语 | 说明 |
| --- | --- |
| **门店 `store`** | 系统以门店为最小经营单元。本项目虽为单品牌，但**全量数据带 `store_id`**，一期只创建一个门店（多门店为预留能力） |
| **堂食 `DINE_IN`** | 顾客在店内就餐，订单必须绑定桌号 |
| **打包 `TAKEAWAY`** | 顾客到店自取，无桌号，系统生成**取餐码**用于叫号取餐 |
| **桌位 `table_info`** | 门店内的餐桌，含桌号、区域、座位数、专属二维码 |
| **桌贴二维码** | 每张桌子一个微信小程序码，扫码后自动进入点餐页并带出桌号 |
| **取餐码** | 打包订单的每日重置流水号，格式 `前缀 + 3 位序号`，如 `A001`。前缀由门店配置，每日 00:00 重置 |
| **规格 SKU** | 菜品的可选属性（如杯型/温度/糖度），规格组 + 规格项组成，规格项可加价 |
| **会员等级** | 按累计成长值划分（如普通/银卡/金卡/黑卡），等级自带专属折扣 |
| **成长值** | 消费产生的等级经验，1 元 = N 成长值（门店可配），仅增不减（取消订单不回退） |
| **积分** | 消费产生的可抵扣资产，1 元 = N 积分（门店可配），可按比例抵扣金额 |
| **优惠券** | 满减券 / 折扣券 / 代金券三种形态 |
| **改单** | 订单进行中，店主对订单菜品进行增、删、改数量的操作，系统记录变更明细 |

---

## 三、顾客端功能需求

### 3.1 登录与授权

#### 3.1.1 静默登录
- 小程序启动时调用 `uni.login()` 获取 `code`，请求服务端 `POST /api/customer/auth/login` 换取 `openid` 与业务 token。
- 服务端若发现 `openid` 未注册，则自动创建用户记录（`user` 表），`phone` 为空。
- 静默登录**不弹窗、不打扰用户**，用于建立会话。

#### 3.1.2 手机号一键授权（关键需求）
- 触发时机（满足任一即触发授权弹窗）：
  1. 点击「去结算」按钮时；
  2. 进入「我的」页面时；
  3. 点击「我的优惠券 / 会员卡」时。
- 实现：使用 `<button open-type="getPhoneNumber" @getphonenumber="...">`，拿到 `code` 后请求 `POST /api/customer/auth/phone`，服务端调用微信「手机号快速验证」接口换取真实手机号。
- 绑定规则：
  - 手机号全局唯一；
  - 若该手机号已存在于其他 `openid` 下（用户换微信），则**合并账号**：将旧用户数据的会员信息、订单归属迁移到当前 openid，或提示用户确认后合并（建议后者更安全）；
  - 用户**拒绝授权**时：可继续浏览菜单、加购物车，但点击「提交订单」时再次引导，最多提示 3 次后可仅提示文字（不可无限弹窗）。
- 手机号展示统一脱敏：`138****8888`。

#### 3.1.3 用户信息
- 昵称与头像：使用 `<button open-type="chooseAvatar">` 与 `<input type="nickname">` 由用户主动填写（微信新规，不再返回真实微信昵称头像）。
- 首次下单前**不强制**填写昵称头像，允许空值，展示为「微信用户」。

### 3.2 扫码点餐入口

| 场景 | 行为 |
| --- | --- |
| 扫桌贴二维码 | 小程序码 scene 携带 `storeId` 与 `tableId`；进入点餐页，自动选中「堂食」并带出桌号（只读，但允许手动切换桌号） |
| 直接打开小程序 | 进入门店首页，默认展示门店信息 + 菜单，订单类型默认「打包」，用户可切换为「堂食」并手动选桌 |
| 二维码无效 / 桌位停用 | 提示 1007「桌号无效，请手动选择桌号或联系店员」，降级为手动选桌 |
| 已有进行中订单再扫码 | 顶部提示「您有 1 个进行中的订单」，提供「查看订单」入口，不阻止新下单 |

**扫描参数设计**（微信小程序码 scene 最多 32 字符）：
```
scene = "s{storeId}t{tableId}"    例：s1t12
```
服务端解析后校验：门店存在且未停用、桌位属于该门店且状态正常。

### 3.3 门店首页与菜单

#### 3.3.1 门店信息展示
- 头部：门店 Logo、店名、营业状态标签（营业中/休息中）、营业时间、公告（跑马灯）。
- 门店简介、地址、电话，点击地址可调起地图导航（`uni.openLocation`）。
- 若门店为「休息中」：菜单可浏览，但「去结算」按钮置灰，提示「门店休息中，暂不可下单」。

#### 3.3.2 菜单浏览（美团式布局）
- **左侧分类导航栏 + 右侧菜品列表**，左右联动（滚动右侧时左侧分类高亮，点击左侧分类时右侧定位）。
- 分类项显示：分类名 + 该分类下菜品数量（可选）。
- 菜品卡片展示：
  - 主图（等比裁切，统一 1:1）
  - 名称（最多 2 行）
  - 标签：招牌 / 新品 / 热销（销量 > 阈值自动标记，阈值门店可配）
  - 规格提示：若菜品有多规格，显示「选规格」按钮，点击弹规格选择弹窗
  - 价格：现价（大字）+ 划线原价（若有 `original_price`）
  - 销量：`已售 N 份`（门店可配置是否展示）
  - 加购按钮：`+` 号；无规格菜品直接加入购物车
- **已售罄 / 已下架菜品**：置灰并显示「已售罄」/ 不展示（门店可配）。
- 分类与菜品均按 `sort` 字段升序，`sort` 相同按 `id` 升序。

#### 3.3.3 菜品详情
- 入口：点击菜品卡片图片或名称。
- 内容：轮播大图（多图）、菜品名称、价格、描述、规格选择、加料选择（P2）、销量、备注输入。
- 规格选择：
  - 规格组内**单选**（如杯型），组与组之间独立选择；
  - 必选规格组未选时「加入购物车」不可点击；
  - 规格项可为默认选中项（`is_default`）；
  - 价格实时联动：`单价 = 菜品价格 + Σ 已选规格项加价`。
- 加入购物车后自动返回菜单页并轻提示。

#### 3.3.4 菜品搜索（P1）
- 顶部搜索框，支持按菜品名称模糊搜索；
- 结果按匹配度排序，展示所属分类。

### 3.4 购物车

| 需求项 | 说明 |
| --- | --- |
| 存储 | 本地存储（`uni.setStorageSync`），key 带 `storeId`，切换门店时清空或隔离 |
| 购物车项唯一性 | `dishId + 规格组合 hash` 唯一；规格不同视为不同项（如「拿铁(大杯/热)」与「拿铁(小杯/冰)」是两项） |
| 底部悬浮栏 | 显示购物车图标、已选件数角标、合计金额、「去结算」按钮 |
| 展开购物车 | 点击图标弹出半屏面板，列出所有已选项，可 `+` / `-` / 删除 / 清空 |
| 数量限制 | 单菜品数量上限 `99` |
| 减至 0 | 自动移除该项 |
| 空购物车 | 「去结算」置灰，点击提示「请先选择商品」 |
| 价格展示 | 显示**商品原价合计**；会员折扣与优惠券在结算页展示（避免与最终价混淆） |
| 库存校验 | 若菜品在结算时已被下架/沽清，提交前二次校验并提示第 1003 号错误，自动移除该菜品 |

### 3.5 下单结算

#### 3.5.1 结算页元素（自上而下）

1. **订单类型切换**：`堂食` / `打包` 分段控件
   - 扫码进入 → 默认 `堂食`，桌号已带出；
   - 非扫码进入 → 默认 `打包`；
   - 切换到「堂食」时必须选择桌号（下拉选择本店可用桌位，按区域分组）；
   - 切换到「打包」时桌号清空，展示「取餐码将在下单后生成」。
2. **就餐人数**（可选，堂食场景，默认 1）
3. **已选菜品列表**：缩略图、名称、规格文案、单价 × 数量、小计、单品备注入口
4. **优惠区**：
   - 优惠券行：显示「N 张可用」或已选券的减免额，点击进入选券弹窗
   - 积分行：显示「可用 X 积分，抵扣 ¥Y」，带开关（默认关闭，由用户主动开启）
   - 会员行：显示「金卡会员 9 折」，不可关闭
5. **整单备注**：文本输入，最多 100 字，示例占位符「口味偏好、忌口等」
6. **价格明细**：
   ```
   商品金额            ¥  68.00
   会员折扣（9折）      -¥   6.80
   优惠券（满50减10）   -¥  10.00
   积分抵扣（500积分）  -¥   5.00
   ──────────────────────────
   应付金额            ¥  46.20
   ```
7. **底部操作栏**：左「应付 ¥46.20」，右「提交订单」按钮
8. **支付方式提示**：明确文案「本店为到店付款，请至柜台支付」

#### 3.5.2 提交校验（服务端必须做）

| 校验项 | 失败错误码 |
| --- | --- |
| 门店营业中 | 1002 |
| 堂食订单已选有效桌号 | 1007 |
| 所有菜品均上架且非沽清 | 1003 |
| 优惠券属于本人、未使用、未过期、满足门槛 | 1005 |
| 积分余额充足且不超过抵扣上限 | 1006 |
| 防重复提交（同用户 5 秒内相同购物车指纹） | 409 |

#### 3.5.3 提交成功
- 生成订单号（规则见 6.4），初始化状态 `PENDING`，`pay_status = UNPAID`。
- 冻结/扣减：优惠券标记占用、积分扣减（在订单完成前不返还，取消时回退）。
- 返回订单 ID，跳转订单详情页，顶部弹出成功提示：
  - 堂食：`下单成功！桌号 A06，请稍候`
  - 打包：`下单成功！取餐码 A012，请留意叫号`
- 店主端触发新订单提醒。

### 3.6 订单管理（顾客端）

#### 3.6.1 订单列表
- Tab 切换：`全部` / `进行中`（PENDING + ACCEPTED + MAKING + READY）/ `已完成` / `已取消`
- 订单卡片：门店名、桌号或取餐码、下单时间、菜品摘要（图片叠放 + 「共 N 件」）、实付金额、状态标签。
- 未完成订单卡片提供操作按钮：
  - `PENDING` → 「取消订单」
  - `ACCEPTED` / `MAKING` → 「催单」（P1，30 分钟内限 1 次）
  - `COMPLETED` → 「再来一单」、「评价」（P2）
- 下拉刷新 + 上拉加载更多。

#### 3.6.2 订单详情
- 顶部状态区：状态名称、状态描述文案、预计等待时间（可选）
- 堂食/打包信息卡：桌号 或 取餐码（取餐码大字展示，便于到柜台出示）
- 菜品明细：每项含图片、名称、规格、加料、数量、单价、小计、单品备注
- 改单提示：若订单被店主修改过，展示黄色提示条「店主已调整菜品：新增 1 项、减少 1 项，最新金额 ¥58.00」，点击可查看改单明细（对比视图）
- 价格明细（与结算页一致）
- 支付信息：支付方式「到店付款」、支付状态（待收款 / 已收款）、收款时间
- 订单信息：订单号（长按复制）、下单时间、备注
- 订单时间轴：下单 → 店主接单 → 开始制作 → 出品完成（待取餐）→ 已完成，每步带时间戳
- 底部操作：按状态渲染（同列表）

#### 3.6.3 取消订单
- `PENDING` 状态：顾客可自助取消，无需店主同意。
- `ACCEPTED` / `MAKING` 状态：需提交取消申请，由店主确认（或直接提示「请联系店员协商」）。
- 取消成功后自动：退回优惠券、退回积分、可选填取消原因。
- `READY` / `COMPLETED` 状态：不可取消。

### 3.7 会员中心

#### 3.7.1 会员卡
- 展示：等级名称、等级图标、专属折扣、成长值进度条（当前/下一等级所需）。
- 数据项：累计消费金额、累计订单数、积分余额、成长值。
- 说明文案：动态展示当前等级权益与升级所需（如「再消费 ¥120 升级金卡，享受 9 折」）。

#### 3.7.2 积分明细
- 分页列表：时间、事项描述（消费获得 / 积分抵扣 / 取消退回 / 店主调整）、变动积分（+/-）、积分余额。

#### 3.7.3 我的优惠券
- Tab：`未使用` / `已使用` / `已过期`
- 券卡片展示：券类型标签、面额（如 `¥10` 或 `9折`）、使用门槛、有效期、适用范围、使用按钮（跳菜单）。
- 即将过期券（7 天内）标红提示。

#### 3.7.4 领券中心（P1）
- 门店可上架的公开领取券列表，每人限领数由券模板控制。
- 「立即领取」按钮，领取后按钮变「已领取」。

### 3.8 个人中心其他

- 头像昵称编辑
- 手机号（脱敏，不可修改；如需换绑，需重新授权）
- 我的订单 / 我的优惠券 / 积分明细 / 会员卡入口
- 「商家入口」按钮 —— **角色切换入口**（见 4.1）
- 关于我们 / 联系客服（调起客服会话 `open-type="contact"`）

---

## 四、店主端功能需求（小程序内）

### 4.1 店主端入口与角色切换

**实现要求（重要）**：
- 店主端页面放在**独立分包** `subpackages/merchant/`，从主包分离以控制体积。
- 主包 `tabBar` 使用 **custom tabBar**（`"custom": true`），根据当前角色动态渲染：
  - 角色为 `CUSTOMER` → 渲染顾客端 Tab（点单 / 订单 / 我的）
  - 角色为 `MERCHANT` → 渲染店主端 Tab（工作台 / 订单 / 管理 / 我的）
- 入口路径：顾客「我的」页面 → 点击「商家入口」→ 若未登录店主账号则跳转店主登录页；已登录则 `switchTab` 进入工作台。
- 店主「我的」页面提供「切换为顾客视角」按钮，返回顾客端。
- 两套 token 独立存储在本地（`customer_token` / `merchant_token`），互不覆盖。

### 4.2 店主登录

- 页面元素：手机号、密码、登录按钮、「忘记密码？请联系管理员」（店主无法自助找回，需超管重置）。
- 登录校验：
  - 账号存在且 `status = 1`；
  - 密码使用 bcrypt 校验；
  - 密码连续错误 5 次锁定 15 分钟（Redis 计数）。
- 登录成功后：
  - 返回 token（payload 含 `storeId`）与门店信息；
  - 校验门店是否存在且未被停用，若停用则返回 2001；
  - 记录 `last_login_at`、`last_login_ip`；
  - 跳转工作台。
- 首次登录强制修改初始密码（`is_init_password = 1` 时）。

### 4.3 工作台（首页）

#### 4.3.1 数据卡片（今日）
| 指标 | 说明 |
| --- | --- |
| 今日订单 | 当日 00:00 至当前的下单数 |
| 今日营业额 | 当日**已收款**订单的实付金额合计 |
| 待接单 | 当前 `PENDING` 订单数（红点 + 数字） |
| 客单价 | 今日实付金额 / 今日有效订单数 |

#### 4.3.2 快捷操作区
- 待接单（带角标）/ 制作中 / 待取餐 —— 点击直达订单列表对应 Tab
- 一键「休息中」开关（快速暂停接单）

#### 4.3.3 新订单提醒（关键体验）
- 轮询方案（默认）：前端每 5 秒调用 `GET /api/merchant/order/pending-count`，数量变化时：
  - 播放提示音（`uni.createInnerAudioContext`，音频文件随包）
  - 顶部弹出横幅「您有新的订单，桌号 A06」
  - 页面震动 `uni.vibrateLong()`
- WebSocket 方案（P2）：升级为实时推送，降低请求量。
- 提醒开关：门店可配置是否开启声音提醒；小程序切到后台时无法播放声音，需在页面 `onShow` 时补拉取一次。

#### 4.3.4 营业状态横幅
- 顶部显示当前营业状态，可一键切换（营业中 ↔ 休息中），切换时二次确认。
- 「休息中」时顾客端无法下单。

### 4.4 订单管理（店主端）

#### 4.4.1 列表
- Tab：`待接单` / `制作中`（ACCEPTED + MAKING）/ `待取餐`（READY）/ `已完成` / `已取消` / `全部`
- 筛选：日期范围（今日/昨日/近 7 天/自定义）、订单类型（堂食/打包）、桌号、订单号/手机号关键词
- 排序：默认按下单时间倒序
- 订单卡片（店主视角额外展示）：
  - 桌号 / 取餐码（大字）
  - 下单时间（含「已等待 X 分钟」）
  - 菜品明细（可折叠，默认展开前 3 项）
  - 整单备注 + 单品备注（**高亮显示**，避免漏看）
  - 金额：原价、优惠、实付
  - 支付状态标签：`待收款` / `已收款`
  - 状态标签
- 超时提醒：`PENDING` 超过 3 分钟未接单，卡片标红（阈值可配）。
- 自动接单（门店可配开关）：开启后新订单直接进入 `ACCEPTED`。

#### 4.4.2 订单操作（按状态渲染）

| 当前状态 | 可用操作 |
| --- | --- |
| `PENDING` | 接单、拒单（填原因）、改单、标记已收款 |
| `ACCEPTED` | 开始制作、改单、取消订单 |
| `MAKING` | 出品完成（→ 待取餐）、改单 |
| `READY` | 确认完成、改单 |
| `COMPLETED` | 查看详情、（若未收款）补标记已收款 |
| `CANCELLED` / `REJECTED` | 仅查看 |

- **接单**：`PENDING → ACCEPTED`，记录 `accepted_at`；顾客端订单状态实时更新。
- **拒单**：必填原因（预设选项：菜品售罄 / 门店太忙 / 顾客信息异常 / 其他 + 自定义），订单置为 `REJECTED`，自动退回优惠券与积分。
- **开始制作**：`ACCEPTED → MAKING`。
- **出品完成**：`MAKING → READY`，顾客端显示「请到柜台取餐」+ 取餐码。
- **确认完成**：`READY → COMPLETED`，触发**会员结算**（详见 7.3）。
- **标记已收款**：`pay_status: UNPAID → PAID`，记录 `paid_at`、`paid_by`（操作人）、`paid_amount`。到店付款场景下这是营收统计的依据。
  - 建议交互：确认完成时若仍未收款，弹出二次确认「该订单尚未标记收款，是否同时标记为已收款？」

#### 4.4.3 改单（核心差异功能）

需求原文：「增加/减少顾客菜品数量」——落地为一个完整的**改单**能力。

**支持的改单操作**：
1. 调整已有菜品数量（+ / −）
2. 删除某个菜品
3. 新增菜品（从本店菜单中选择，同样支持规格选择）
4. 修改单品备注
5. 整单改价/折扣（P1：整单折扣、抹零、自定义立减）
6. 修改订单类型与桌号（堂食↔打包，需重新生成/释放取餐码）

**改单规则**：
- 已 `COMPLETED` 或 `CANCELLED` 的订单**禁止改单**。
- 改单后**重新计算价格**：商品金额 → 会员折扣 → 优惠券（重新校验是否仍满足门槛，不满足则自动移除并提示）→ 积分抵扣（不可超过新金额上限，超出部分自动回退）→ 应付金额。
- 若改单导致金额**下降**且订单已标记收款 → 提示店主「需线下退款 ¥X」并记录退款金额。
- 每次改单生成一条 `order_modify_log`，保存改单前后快照（JSON）、金额变化、操作人、时间。
- 顾客端订单详情顶部出现改单提示条，可查看改单明细（对比视图）。
- 整个改单过程在一个页面内完成：`订单详情 → 点击「改单」→ 改单页（菜品列表可编辑 + 底部实时总价 + 保存按钮）`。

#### 4.4.4 订单搜索与其他
- 按订单号精确搜索。
- 按顾客手机号搜索（脱敏输入后四位也可匹配，可选）。
- 订单导出 Excel（P1，需权限控制）。

### 4.5 菜单管理

#### 4.5.1 分类管理
- 列表：分类名称、图标、菜品数量、排序值、状态（启用/停用）、操作。
- 新增/编辑：名称（必填，≤ 20 字）、图标（上传，建议 1:1）、排序值、状态。
- 删除：若分类下存在菜品，禁止删除并提示（或二次确认后连带下架菜品 —— 建议**禁止**，更安全）。
- 排序：支持拖拽或上下箭头调整，保存后批量提交 `sort`。

#### 4.5.2 菜品管理

**列表**：
- 按分类 Tab 筛选，或下拉选择分类
- 搜索框（按名称）
- 状态筛选：全部 / 上架 / 下架 / 已沽清
- 每项展示：图片、名称、价格、销量、状态、操作按钮（编辑 / 上下架 / 沽清 / 删除）
- 支持批量操作：批量上架 / 批量下架 / 批量删除（P1）

**新增 / 编辑菜品表单**：

| 字段 | 必填 | 说明 |
| --- | :-: | --- |
| 菜品名称 | ✅ | ≤ 30 字 |
| 所属分类 | ✅ | 下拉选择已启用分类 |
| 主图 | ✅ | 上传 1 张，建议 800×800，≤ 2MB |
| 详情图集 | ❌ | 最多 5 张，用于详情页轮播 |
| 菜品描述 | ❌ | ≤ 200 字 |
| 售价 | ✅ | 正数，两位小数 |
| 划线原价 | ❌ | 需 > 售价 |
| 单位 | ❌ | 如「杯」「份」，默认「份」 |
| 初始销量（销量基数） | ❌ | 用于营造销量，展示销量 = 基数 + 真实销量 |
| 标签 | ❌ | 多选 + 自定义：招牌 / 新品 / 推荐 等 |
| 排序值 | ❌ | 数字，越小越前 |
| 库存模式 | ✅ | 不限量 / 每日限量（填数量） |
| 沽清状态 | - | 今日售罄开关（每日自动重置，需定时任务） |
| 上架状态 | ✅ | 上架 / 下架 |

**规格设置（SKU）**：
- 可添加多个规格组，每组：组名（如「杯型」）、是否必选、是否多选、排序。
- 每组下可添加多个规格项：名称（如「中杯」「大杯」）、加价（可为 0 或负数）、是否默认选中。
- 支持从「规格模板」快速套用（P1），如预设「杯型/温度/糖度」三组。
- 校验：至少保留 1 个规格项才能启用规格组；已产生订单的规格项建议只停用不删除（历史订单快照不受影响）。

**加料/小料（P2）**：门店级加料库（如「燕麦奶 +3 元」），下单时可选，计入订单明细。

#### 4.5.3 图片上传
- 统一使用 `POST /api/merchant/upload`，服务端压缩后存储（建议长边 ≤ 1200px，质量 80%）。
- 支持一次选 1 张（封面）或多张（图集）。
- 上传失败可重试；上传中显示进度。

### 4.6 桌位管理

| 功能 | 说明 |
| --- | --- |
| 桌位列表 | 桌号、区域、座位数、二维码状态、启用状态、操作 |
| 新增桌位 | 桌号（唯一，≤ 10 字）、区域（如 A 区/露台，可自定义）、座位数、排序 |
| 编辑 / 删除 | 删除为软删；有进行中订单的桌位禁止删除，提示 |
| 批量新增 | 支持「A01-A10」批量生成（P1） |
| 生成二维码 | 点击生成该桌的微信小程序码（`wxacode.getUnlimited`），scene = `s{storeId}t{tableId}` |
| 二维码预览与下载 | 弹窗展示二维码 + 桌号文字，提供「保存到相册」/「复制链接」 |
| 批量导出 | 一次性生成所有桌位二维码图片压缩包（P1） |
| 启用 / 停用 | 停用后扫码提示桌号无效 |

> 二维码生成后需缓存 `qrcode_url`，避免重复调用微信接口（有频次限制）。

### 4.7 营销管理

#### 4.7.1 优惠券管理

**券类型**：
| 类型 | 代码 | 示例 | 计算方式 |
| --- | --- | --- | --- |
| 满减券 | `FULL_REDUCE` | 满 50 减 10 | 订单金额 ≥ 门槛则减固定额 |
| 折扣券 | `DISCOUNT` | 8.5 折 | 订单金额 × 折扣率，可设最高减免额 |
| 代金券 | `CASH` | 无门槛减 5 | 订单金额 ≥ 门槛（可为 0）则减固定额 |

**创建表单字段**：

| 字段 | 说明 |
| --- | --- |
| 券名称 | 如「开业满减券」 |
| 券类型 | 三选一 |
| 面额 / 折扣 | 按类型动态显示 |
| 使用门槛 | 满 X 元可用，0 表示无门槛 |
| 最高减免 | 仅折扣券需填，如「8.5 折，最高减 20 元」 |
| 有效期类型 | 固定日期（开始~结束）/ 领取后 N 天有效 |
| 发放总量 | 0 表示不限量 |
| 每人限领 | 默认 1 张 |
| 适用范围 | 全场 / 指定分类 / 指定菜品（P1） |
| 发放方式 | 用户主动领取 / 定向发放（按手机号）/ 新人自动发 / 满额自动发（P2） |
| 使用说明 | 自定义文案，展示在券面 |
| 状态 | 未开始 / 进行中 / 已结束 / 已停用 |

**券管理操作**：
- 列表：券名、类型、面额、已领取/已使用/核销率、有效期、状态、操作
- 编辑：进行中的券**仅允许**修改有效期延长、发放总量、每人限领、状态；面额与门槛不可改（避免已领券争议）—— 或允许改但需二次强确认（建议前者）
- 停用 / 删除（软删，已发放的券不受影响）
- 定向发券：选择券 → 输入手机号（支持多行批量）→ 发放，记录 `coupon_grant_log`
- 领取/核销记录：分页展示领取人、领取时间、使用时间、关联订单

#### 4.7.2 会员等级配置

| 字段 | 说明 |
| --- | --- |
| 等级名称 | 如 普通会员 / 银卡 / 金卡 / 黑卡 |
| 等级值 | 1、2、3、4，用于排序与比较 |
| 升级门槛（成长值） | 达到该成长值自动升级 |
| 专属折扣 | 如 1.00 / 0.95 / 0.90 / 0.85 |
| 等级图标 | 上传 |
| 权益说明 | 文案，展示在会员卡 |
| 状态 | 启用 / 停用 |

规则：
- 系统必须存在一个最低等级（成长值门槛 0），不可删除。
- 成长值只升不降（不支持降级，简化逻辑；如需降级可后续加周期规则）。
- 等级变更在下单结算时实时生效（按当前等级折扣计算）。
- 门店可一键「使用平台模板初始化」，超管在后台维护模板。

#### 4.7.3 积分规则配置
| 配置项 | 默认值 | 说明 |
| --- | --- | --- |
| 积分开关 | 开 | 关闭后不产生也不可抵扣积分 |
| 消费积分比例 | 1 元 = 1 积分 | 按**实付金额**取整计算 |
| 积分抵扣比例 | 100 积分 = 1 元 | - |
| 单笔抵扣上限 | 订单金额 30% | 避免大额薅羊毛，可为 0 表示不限 |
| 积分抵扣最小单位 | 100 积分 | 抵扣按 100 的整数倍 |

#### 4.7.4 会员列表与详情

**列表**：手机号（脱敏）、昵称、等级、累计消费、积分余额、订单数、最近下单时间、注册时间。
- 支持按手机号搜索、按等级筛选、按累计消费排序。
- 导出 Excel（P1）。

**详情**：
- 基础信息 + 会员卡
- 消费记录（该会员在本店的订单列表，点击可跳转订单详情）
- 积分明细
- 持有优惠券
- 手动调整积分 / 成长值：填写变动值（可负）+ 原因（必填），生成 `points_log`（类型 `ADMIN`）

### 4.8 店铺设置

| 分组 | 字段 |
| --- | --- |
| 基础信息 | 门店名称、Logo、封面图、简介（≤ 500 字）、公告（≤ 100 字）、联系电话 |
| 地址 | 省市区、详细地址、经纬度（地图选点 `uni.chooseLocation`） |
| 营业 | 营业时间（开始/结束）、营业状态（营业中/休息中） |
| 下单 | 是否开放堂食、是否开放打包、是否必须扫码才能点堂食、是否开启自动接单、是否展示销量、是否展示已售罄菜品 |
| 取餐码 | 前缀（1~2 位字母）、是否每日重置 |
| 会员 | 成长值比例（1 元 = N 成长值）、积分比例、抵扣比例、抵扣上限、会员功能总开关 |
| 提醒 | 新订单声音提醒开关、超时未接单阈值（分钟） |
| 桌位 | 是否允许顾客手动选桌位 |

> 保存后即时生效，顾客端下次请求即读到新配置。

### 4.9 数据统计（店主端）

| 模块 | 内容 |
| --- | --- |
| 概览 | 所选时间范围内的订单总数、已完成数、已取消数、营业额（已收款）、客单价、优惠总额 |
| 趋势图 | 折线图：订单量 + 营业额 按天展示 |
| 菜品排行 | 横向柱状图：销量 TOP 10 菜品 |
| 订单构成 | 饼图：堂食 / 打包 占比 |
| 时段分布 | 柱状图：按小时统计下单量（P1） |
| 会员 | 新增会员数、会员消费占比（P1） |
| 时间范围 | 今日 / 昨日 / 近 7 天 / 近 30 天 / 自定义 |

---

## 五、超级管理员后台功能需求（Web）

### 5.1 登录与安全

- 登录页：账号、密码、图形验证码（服务端生成，2 分钟有效）。
- 连续 5 次密码错误，账号锁定 15 分钟。
- 登录成功签发 2 小时有效 token，操作 30 分钟无请求则强制退出（前端空闲检测）。
- 预留 RBAC 模型（`role` 字段），一期只实现 `SUPER_ADMIN`。

### 5.2 门店管理

| 功能 | 说明 |
| --- | --- |
| 门店列表 | 门店名称、Logo、负责人（关联店主）、联系电话、所在城市、营业状态、创建时间、操作 |
| 搜索筛选 | 按门店名称搜索、按状态筛选 |
| 新增门店 | 门店名称（必填、唯一）、Logo、封面、简介、联系电话、省市区 + 详细地址、经纬度、营业时间、状态 |
| 编辑门店 | 同新增（不含门店 ID 变更） |
| 启用 / 停用 | 停用后该门店店主无法登录（返回 2001），顾客端提示门店不可用 |
| 删除门店 | 软删除；存在订单数据时二次强确认 |
| 门店详情 | 基础信息 + 该店统计概览（订单数、营业额、会员数、菜品数）+ 该店店主账号列表 |

### 5.3 店主账号管理

| 功能 | 说明 |
| --- | --- |
| 账号列表 | 手机号、姓名、绑定门店、角色（店主/店员）、状态、最后登录时间、最后登录 IP、创建时间、操作 |
| 搜索筛选 | 按手机号/姓名搜索，按门店筛选，按状态筛选 |
| 新增账号 | 手机号（唯一、格式校验）、姓名、绑定门店（下拉选择）、角色（OWNER/STAFF）、初始密码（可自动生成，展示一次并提供复制） |
| 编辑账号 | 姓名、绑定门店（换绑）、角色 |
| 重置密码 | 生成新随机密码，展示一次并提供复制；同时置 `is_init_password = 1`，强制下次登录修改 |
| 禁用 / 启用 | 禁用后立即失效（token 校验时二次查库校验 `status`） |
| 删除账号 | 软删除 |
| 解绑门店 | 需二次确认，解绑后账号不可登录 |

> **关键约束**：一个门店至少保留 1 个启用状态的 `OWNER` 账号，删除/禁用最后一个店主时禁止并提示。

### 5.4 平台数据看板

| 模块 | 内容 |
| --- | --- |
| 全局概览卡片 | 门店总数、启用店主数、今日总订单、今日总营业额、会员总数、今日新增会员 |
| 订单趋势 | 折线图：近 30 天（可切换近 7 天）每日订单量、营业额双轴 |
| 门店排行 | 横向柱状图 TOP 10：按营业额 / 按订单量可切换 |
| 订单构成 | 饼图：堂食 / 打包 占比；可叠加各门店占比 |
| 菜品排行 | 跨门店菜品销量 TOP 10（可筛选门店） |
| 会员增长 | 折线图：近 30 天新增会员数 |
| 筛选与下钻 | 时间范围筛选、门店筛选、点击图表可下钻到明细列表 |
| 数据导出 | 导出 Excel（订单明细、门店汇总、菜品销量） |
| 跨门店订单查询 | 全局订单列表（门店、订单号、顾客手机号、桌号/取餐码、金额、状态、时间），支持导出 |

### 5.5 全局配置

| 分组 | 配置项 |
| --- | --- |
| 平台信息 | 平台名称、平台 Logo、客服电话、版权信息 |
| 会员等级模板 | 维护一套默认等级模板（名称、门槛、折扣、权益），门店可一键套用 |
| 积分默认规则 | 默认消费积分比例、抵扣比例、抵扣上限（新门店创建时写入默认值） |
| 提示文案 | 首页公告、下单成功文案、休息中文案等（P2） |
| 文件存储 | 存储类型、COS/OSS 配置（敏感信息加密存储，P1） |

### 5.6 系统管理

| 功能 | 说明 |
| --- | --- |
| 操作日志 | 记录三端关键操作：操作人类型、操作人、门店、模块、动作、目标 ID、内容、IP、时间；支持筛选与导出 |
| 登录日志 | 登录成功/失败记录（超管 + 店主） |
| 文件管理 | 上传文件列表，含大小、上传者、业务类型，可删除无用文件 |
| 数据字典 | 维护可配置枚举（如区域、标签），P2 |

---

## 六、核心业务流程与订单状态机

### 6.1 顾客点餐主流程

```
顾客扫描桌贴二维码
   │
   ├─ 解析 scene → 校验门店 & 桌位 → 进入点餐页（默认堂食 + 桌号）
   │
   ├─ 静默登录（openid）→ 会话建立
   │
   ├─ 浏览菜单 → 选规格 → 加入购物车
   │
   ├─ 点击「去结算」
   │     └─ 未绑定手机号 → 弹出手机号授权 → 绑定成功继续
   │
   ├─ 结算页：选订单类型（堂食/打包）→ 选优惠券 → 开启积分抵扣 → 填备注
   │
   ├─ 提交订单 → 服务端校验 → 生成订单（PENDING / UNPAID）
   │     └─ 展示桌号或取餐码
   │
   └─ 店主端收到新订单提醒
```

### 6.2 订单履约流程

```
PENDING（待接单）
   │  店主接单
   ▼
ACCEPTED（已接单）
   │  店主开始制作
   ▼
MAKING（制作中）
   │  出品完成
   ▼
READY（待取餐）       ← 顾客收到「请到柜台取餐」提示
   │  顾客取餐 / 店主确认完成
   ▼
COMPLETED（已完成）   ← 触发会员结算（成长值 + 积分）
                         店主标记「已收款」（到店付款）
```

### 6.3 订单状态机（严格定义）

**订单状态枚举 `order_main.status`**：

| 状态 | 代码 | 说明 | 终态 |
| --- | --- | --- | :-: |
| 待接单 | `PENDING` | 已提交，等待店主处理 | 否 |
| 已接单 | `ACCEPTED` | 店主已接单 | 否 |
| 制作中 | `MAKING` | 店主开始制作 | 否 |
| 待取餐 | `READY` | 出品完成，等待顾客取餐 | 否 |
| 已完成 | `COMPLETED` | 交易完成 | 是 |
| 已取消 | `CANCELLED` | 顾客或店主主动取消 | 是 |
| 已拒单 | `REJECTED` | 店主拒绝接单 | 是 |

**支付状态枚举 `order_main.pay_status`**：
| 状态 | 代码 | 说明 |
| --- | --- | --- |
| 待收款 | `UNPAID` | 默认，到店付款 |
| 已收款 | `PAID` | 店主在店主端标记 |

**流转规则表**（服务端必须严格校验，非法流转一律返回 `1004`）：

| # | 当前状态 | 操作 | 目标状态 | 操作者 |
| --- | --- | --- | --- | --- |
| 1 | `PENDING` | 接单 | `ACCEPTED` | 店主 |
| 2 | `PENDING` | 拒单 | `REJECTED` | 店主 |
| 3 | `PENDING` | 取消 | `CANCELLED` | 顾客 / 店主 |
| 4 | `ACCEPTED` | 开始制作 | `MAKING` | 店主 |
| 5 | `ACCEPTED` | 取消 | `CANCELLED` | 店主（或协商） |
| 6 | `MAKING` | 出品完成 | `READY` | 店主 |
| 7 | `READY` | 确认完成 | `COMPLETED` | 店主 |
| 8 | `READY` | 取消（顾客未取） | `CANCELLED` | 店主 |

**支付状态流转**：
| 当前 | 操作 | 目标 | 操作者 |
| --- | --- | --- | --- |
| `UNPAID` | 标记已收款 | `PAID` | 店主 |
| `PAID` | 取消/退款 | 记录退款金额，状态回到 `UNPAID` 或保持 `PAID` 并记 `refund_amount` | 店主 |

**每次状态变更都必须写入 `order_log`**（操作者类型、操作者 ID、原状态、新状态、时间、备注）。

### 6.4 订单号与取餐码生成规则

**订单号**：`{yyyyMMddHHmmss}{storeId 后 2 位}{4 位随机数}`
例：`20260914153205` + `01` + `4823` → `20260914153205014823`（共 20 位，全局唯一索引）

**取餐码**：`{前缀}{3 位当日序号}`，例 `A001`、`A012`
- 前缀取门店 `store.pickup_code_prefix`（默认 `A`）
- 序号每日 00:00 重置，由 `daily_sequence` 表按 `(store_id, biz_type, seq_date)` 原子递增生成
- 序号超过 999 时循环补位（`A1000` 起变为 4 位），但同一时刻不允许重复
- `daily_sequence` 表按 (store_id, biz_type, seq_date) 唯一，使用 `UPDATE ... SET current_value = current_value + 1` 或 `INSERT ... ON DUPLICATE KEY UPDATE` 保证并发安全

### 6.5 改单流程

```
店主订单详情 → 点击「改单」
   │
   ├─ 进入改单页（当前订单菜品可编辑）
   │     ├─ 调整数量 / 删除菜品
   │     ├─ 「+ 添加菜品」→ 从本店菜单选择（含规格选择）
   │     └─ 修改单品备注 / 整单备注
   │
   ├─ 底部实时展示：原金额 → 新金额（含差额提示）
   │
   ├─ 保存 → 二次确认弹窗（展示变更摘要）
   │     └─ 若金额下降且已收款 → 额外提示「需线下退款 ¥X」
   │
   └─ 保存成功 → 写入 order_modify_log → 订单金额更新 → 顾客端可见改单提示
```

### 6.6 优惠券与积分生命周期

**优惠券**：
```
创建（券模板 coupon）→ 发放（用户领取/定向发放 → user_coupon.UNUSED）
   → 下单占用（结算时校验并锁定）
   → 订单完成 → 核销（user_coupon.USED，记录 order_id、used_at）
   → 订单取消 → 退回（回到 UNUSED）
   → 超过 expire_at → 定时任务置为 EXPIRED
```

**积分**：
```
订单完成 → 累积（EARN，按实付金额 × 比例）
下单抵扣 → 扣减（DEDUCT，先校验余额与上限）
订单取消 → 退回（REFUND）
店主调整 → ADMIN（正负均可，需填原因）
```

---

## 七、价格与营销计算规则

### 7.1 计算公式（服务端唯一权威，前端仅回显）

```
① 商品金额 = Σ（每项单价 × 数量）              ← 单价 = 菜品售价 + Σ所选规格项加价
② 会员折扣额 = 商品金额 × (1 - 会员折扣率)
③ 券后金额 = 商品金额 - 会员折扣额
④ 优惠券抵扣额：
     满减/代金券 → 固定面额（需 ③ ≥ 门槛）
     折扣券     → ③ × (1 - 折扣率)，且不超过最高减免额
⑤ 积分抵扣额 = min（可用积分 ÷ 抵扣比例, ④ × 抵扣上限比例）
     （按最小抵扣单位取整）
⑥ 应付金额 = max(③ - ④ - ⑤, 0.01)             ← 最低 0.01 元，避免 0 元订单
```

**计算顺序（固定不可变）**：商品金额 → 会员折扣 → 优惠券 → 积分抵扣。

**规则**：
- 所有金额以「分」为单位参与运算（`Math.round(x * 100)`），最后再除以 100，避免浮点误差。
- 会员折扣与优惠券**可叠加**（门店可通过配置改为互斥）。
- 差额分摊：当优惠券需按菜品分摊时（用于退款/改单场景），按各项小计金额比例分摊，尾差归入金额最大的一项。

### 7.2 会员等级计算

```
订单 COMPLETED 时：
  growth  += floor(实付金额 × 门店成长值比例)    // 1 元 = N 成长值，默认 1
  points  += floor(实付金额 × 门店积分比例)      // 1 元 = N 积分，默认 1
  total_consume += 实付金额
  order_count += 1
  → 查询 growth 是否达到下一等级门槛 → 是则升级（level_id 变更），并记录日志
```
- 成长值与积分按**实付金额**计算（已扣除优惠），避免刷单。
- 订单取消：不结算；若已结算则回退积分与成长值（成长值回退按门店配置，默认回退）。

### 7.3 积分抵扣规则

- 抵扣开关默认**关闭**，需用户主动开启（避免误操作）。
- 用户输入或系统自动取「可抵扣最大值」= min（积分余额, 上限额 × 抵扣比例），按最小单位取整。
- 抵扣后剩余应付金额若为 0，仍按 0.01 元记录。
- 积分不足时提示 1006，不自动降级。

### 7.4 价格明细展示结构（接口返回统一结构）

```json
{
  "goodsAmount": 68.00,
  "memberDiscount": 6.80,
  "memberLevelName": "金卡会员",
  "memberDiscountRate": 0.9,
  "couponDiscount": 10.00,
  "couponName": "满50减10",
  "pointsUsed": 500,
  "pointsDiscount": 5.00,
  "payAmount": 46.20,
  "details": [
    { "label": "商品金额", "value": 68.00, "type": "ADD" },
    { "label": "会员折扣（9折）", "value": -6.80, "type": "SUB" },
    { "label": "优惠券（满50减10）", "value": -10.00, "type": "SUB" },
    { "label": "积分抵扣（500积分）", "value": -5.00, "type": "SUB" }
  ]
}
```

---

## 八、页面清单

### 8.1 顾客端页面（主包 `pages/`）

| 页面路径 | 页面名称 | 主要功能 |
| --- | --- | --- |
| `pages/index/index` | 门店首页 | 门店信息、公告、推荐菜品、进入点餐 |
| `pages/order/menu` | 点餐页（核心） | 分类导航 + 菜品列表 + 购物车栏 + 结算入口 |
| `pages/order/dish-detail` | 菜品详情 | 图集、描述、规格选择、加入购物车 |
| `pages/order/cart` | 购物车（或半屏弹窗） | 已选菜品管理 |
| `pages/order/confirm` | 确认订单 | 类型切换、桌号/取餐码、券、积分、备注、价格明细 |
| `pages/order/detail` | 订单详情 | 状态、菜品、改单提示、价格、时间轴、操作 |
| `pages/order/list` | 我的订单 | 状态 Tab、订单卡片 |
| `pages/mine/index` | 个人中心 | 用户信息、会员卡、入口聚合 |
| `pages/mine/member` | 会员中心 | 等级、成长值、权益 |
| `pages/mine/points` | 积分明细 | 积分流水 |
| `pages/mine/coupon` | 我的优惠券 | 未使用/已使用/已过期 |
| `pages/coupon/center` | 领券中心 | 可领取券列表 |
| `pages/login/phone` | 手机号授权引导 | 授权按钮 + 说明 |
| `pages/common/webview` | 通用 webview | 协议、帮助文档 |
| `pages/merchant-entry` | 商家入口中转 | 校验店主登录态并跳转分包 |

### 8.2 店主端页面（分包 `subpackages/merchant/`）

| 页面路径 | 页面名称 | 主要功能 |
| --- | --- | --- |
| `login/index` | 店主登录 | 手机号 + 密码 |
| `login/change-password` | 修改密码 | 首次登录强制修改 |
| `dashboard/index` | 工作台 | 今日数据、待接单角标、快捷操作、营业开关 |
| `order/list` | 订单列表 | 状态 Tab、筛选、新订单提醒 |
| `order/detail` | 订单详情 | 全量信息、状态操作、改单入口、标记收款 |
| `order/modify` | 改单页 | 增删菜品、改数量、实时金额 |
| `order/logs` | 订单日志 | 状态流转记录 |
| `dish/category` | 分类管理 | 分类 CRUD 与排序 |
| `dish/list` | 菜品列表 | 筛选、搜索、批量操作 |
| `dish/edit` | 菜品新增/编辑 | 表单 + 规格设置 + 图片上传 |
| `dish/spec-setting` | 规格设置 | 规格组与规格项维护 |
| `table/list` | 桌位管理 | 桌位 CRUD |
| `table/qrcode` | 二维码管理 | 生成、预览、下载 |
| `marketing/coupon/list` | 优惠券列表 | 券 CRUD、状态管理 |
| `marketing/coupon/edit` | 优惠券编辑 | 券配置表单 |
| `marketing/coupon/records` | 领取核销记录 | 明细列表 |
| `marketing/member/list` | 会员列表 | 搜索、筛选、导出 |
| `marketing/member/detail` | 会员详情 | 消费记录、积分、调分 |
| `marketing/member/level` | 等级配置 | 等级 CRUD、折扣设置 |
| `marketing/points-rule` | 积分规则 | 比例与上限配置 |
| `stat/index` | 数据统计 | 概览、趋势、排行、构成 |
| `store/setting` | 店铺设置 | 全部店铺配置项 |
| `mine/index` | 店主中心 | 账号信息、切换顾客视角、退出登录 |

### 8.3 管理后台页面（Web）

| 路由 | 页面名称 |
| --- | --- |
| `/login` | 登录页 |
| `/dashboard` | 平台数据看板 |
| `/store/list` | 门店列表 |
| `/store/edit/:id?` | 门店新增/编辑 |
| `/store/detail/:id` | 门店详情（含统计） |
| `/store-user/list` | 店主账号列表 |
| `/order/list` | 跨门店订单查询 |
| `/member-level/template` | 会员等级模板 |
| `/config/platform` | 平台信息配置 |
| `/config/points` | 积分默认规则 |
| `/system/log/operation` | 操作日志 |
| `/system/log/login` | 登录日志 |
| `/system/file` | 文件管理 |

---

## 九、视觉与交互设计规范

> 完整设计规范见 **`04-视觉设计规范.md`**（含设计令牌、组件规范、页面级风格指引、uni.scss 变量）。本节为需求层面的约束摘要，设计与开发必须同时满足。

### 9.1 风格定位

- 整体视觉风格**参考 HelloKitty 的粉白配色、圆润造型与甜美氛围**，定位为**日系可爱风（Kawaii）**。
- 品牌自有吉祥物为**猫咪 IP「爱咪」**，与「爱猫咖啡」的猫主题一致。

### 9.2 ⚠️ IP 合规红线（硬性约束）

**HelloKitty（凯蒂猫）是日本三丽鸥（Sanrio）的注册商标与著作权形象，商用需授权。本项目只参考其风格语言，不得使用其形象。**

| 禁止事项 | 说明 |
| --- | --- |
| ❌ 使用 HelloKitty 官方形象、贴图、表情包 | 著作权侵权 |
| ❌ 使用「HelloKitty」「凯蒂猫」「Kitty」作为品牌名、菜品名、券名 | 商标侵权 |
| ❌ 使用三丽鸥官方字体、Logo、蝴蝶结图形 | 商标 / 著作权侵权 |
| ❌ 绘制与 HelloKitty 经典造型「实质性相似」的形象 | 仍有侵权风险 |
| ❌ 直接下载网络二创图作为素材 | 来源无授权，风险不可控 |
| ✅ 采用**自研猫咪 IP「爱咪」**（白猫 + 猫耳猫爪 + 咖啡师元素） | 正确做法 |

- 所有插画、图标、素材必须为**原创**或**明确商用授权**，并在 `upload_file` 表记录来源与授权信息。
- 建议为自研猫咪 IP 申请**美术作品著作权登记 + 商标注册**，把风险点转化为品牌资产。

### 9.3 核心视觉约束（三端必须遵守）

| 类别 | 要求 |
| --- | --- |
| 主色 | 品牌主粉 `#FF8FB1`；按压态 `#E8618C`；浅粉底 `#FFE1EC`；页面背景 `#FFF6F9` |
| 点缀色 | 蝶结黄 `#FFDC7A`（浅，用于标签底 / 角标底 / 装饰块）、婴儿蓝 `#A9D9EA`；**蝶结黄底上必须用深色文字，禁止白字** |
| 状态色 | 三端订单状态色统一固定（待接单粉 / 已接单橙 / 制作中蓝 / 待取餐绿 / 已完成灰粉 / 已取消灰） |
| 造型 | **圆润无锐角**：卡片圆角 24rpx、按钮胶囊 999rpx，禁止直角元素 |
| 阴影 | 一律使用**粉色调阴影**，禁止 `rgba(0,0,0,...)` 黑色阴影 |
| 字体 | 中文用系统圆润字体（PingFang SC）；**不使用可爱字体写正文**；数字加粗 |
| 图标 | 线性圆头风格，线宽统一，选中态用主粉 |
| 背景 | 页面底色用浅粉白 `#FFF6F9`，**不使用纯白与大面积粉色** |
| 可读性 | 粉色背景上文字对比度：大字号 ≥ 3:1、正文 ≥ 4.5:1；**禁止浅粉底白字、禁止主粉写正文** |
| 粉色占比 | 顾客端单屏粉色（含浅粉）面积 **≤ 40%**；店主端 ≤ 15% |
| 深色模式 | 本期不做，小程序**锁定浅色外观** |

### 9.4 三端风格差异化（关键）

顾客端是**营销界面**可以甜；店主端与后台是**生产工具**必须冷静高效。三者共用同一色板与圆角，但粉色使用比例与饱和度不同。

| 端 | 风格策略 |
| --- | --- |
| **顾客端** | 粉白主调，强可爱氛围。首页粉色云朵渐变背景 + 猫咪探头；菜单双栏；购物车猫爪图标；下单成功页猫咪插画 + 爱心粒子；会员卡粉白渐变 + 蝴蝶结暗纹 |
| **店主端** | **可爱度降级**。白底为主，粉色仅用于按钮、选中态、状态标签与数字；数据卡片改为白底细粉边 + 无阴影；**禁止**使用猫咪大插画、圆体字、渐变背景块；保留唯二的两个可爱元素：新订单提醒横幅（猫爪 + 粉色底）与圆形步进器；关闭装饰性动效 |
| **管理后台** | 粉**仅作主色点缀**。覆盖 Element Plus `--el-color-primary: #FF8FB1`；侧边栏白底 + 选中浅粉底，不用深色侧边栏；内容区背景用中性 `#F7F8FA`；仅在**登录页**允许使用猫咪 IP 与粉色渐变 |

### 9.5 关键交互反馈要求

| 场景 | 要求 |
| --- | --- |
| 加入购物车 | 猫爪图标弹跳 + 购物车角标同步变化（200ms） |
| 下单成功 | 猫咪插画 + 爱心粒子动画（≤ 800ms），并大字展示桌号 / 取餐码 |
| 新订单到达（店主端） | 顶部粉色横幅下滑 + 提示音 + 震动，三感齐发，**不可省略** |
| 空状态 | 必须有猫咪插画 + 治愈系文案 + 主按钮（如购物车为空：「购物车还空空的，爱咪在等你点单呀～」） |
| 加载中 | 猫爪旋转动画 + 文案「爱咪正在准备…」 |
| 骨架屏 | 使用浅粉色 shimmer 扫光，**不使用灰色骨架** |
| 底部弹窗 | 顶部圆角 32rpx + 暖调遮罩 `rgba(81,63,71,0.45)` |

### 9.6 视觉资产清单（需设计产出）

| 资产 | 数量 | 说明 |
| --- | --- | --- |
| 猫咪 IP「爱咪」主形象 | 1 套 | 三视图 / 六表情 |
| 猫咪场景插画 | 6 张 | 空购物车、无订单、无优惠券、加载中、加载失败、下单成功 |
| 功能图标 | 约 40 个 | 线性圆头，含猫爪、蝴蝶结、咖啡杯等专属元素 |
| 会员卡背景 | 1 张 | 粉白渐变 + 蝴蝶结暗纹 + 猫爪水印 |
| 桌贴二维码模板 | 1 版 | A6 打印尺寸，含桌号大字 + 小程序码 + 猫咪形象 |
| 启动页 | 1 张 | 猫咪 IP + 品牌名 + 粉色云朵 |
| 分享卡片 | 1 张 | 用于小程序分享 |
| 表情包 | 6~12 个 | P2 扩展 |

---

## 十、非功能需求

### 10.1 性能
- 菜单接口响应时间 P95 < 300ms（含菜品与规格，建议 Redis 缓存，菜品变更时失效）。
- 订单提交接口 P95 < 500ms，需使用数据库事务保证一致性。
- 新订单提醒轮询间隔 5 秒，接口响应 < 100ms。
- 列表接口统一分页，单页最大 50 条。
- 图片走 CDN 或静态服务，前端使用 `lazy-load`。

### 10.2 安全
- 所有接口鉴权，店主端接口强制 `storeId` 隔离（中间件注入，禁止前端传参决定数据范围）。
- 敏感信息脱敏：手机号返回统一 `138****8888`。
- 密码 bcrypt（cost 10），禁止明文与 MD5。
- 数据库连接参数放 `.env`，不提交仓库；提供 `.env.example`。
- 防 SQL 注入（ORM 参数化）、防 XSS（富文本过滤）、防重放（同请求 5 秒内指纹去重）。
- 下单接口做频率限制：单用户 10 秒内最多 1 次提交。
- 上传接口校验文件类型与大小（仅 jpg/png/webp，≤ 5MB）。
- 超管后台所有写操作记录操作日志。

### 10.3 数据一致性
- 订单创建、改单、状态流转、券核销、积分变动必须在**同一数据库事务**内完成。
- 订单金额相关字段（`total_amount`、`pay_amount`、各优惠额）在订单表中冗余存储，避免历史价格被菜单改价影响。
- 订单明细需保存菜品**快照**（名称、图片、单价、规格文案），菜单后续修改不影响历史订单。

### 10.4 兼容与体验
- 小程序基础库 ≥ 2.25.0（手机号授权组件要求）。
- 适配 iPhone 刘海屏与底部安全区（`env(safe-area-inset-bottom)`）。
- 空状态、加载中、网络异常均有明确反馈。
- 所有可点击区域 ≥ 44×44px。
- 下单、接单等关键操作有明确的成功/失败反馈。

### 10.5 可运维性
- 服务端统一日志（请求日志 + 错误日志），错误日志含请求 ID 便于排查。
- 每日凌晨执行定时任务：取餐码序号重置、优惠券过期处理、日统计数据落表。
- 数据库每日自动备份。

---

## 十一、需求边界（本期不做）

| 需求 | 说明 |
| --- | --- |
| 微信支付在线付款 | 本期为到店付款，仅预留 `pay_status` 与支付扩展位 |
| 外卖配送 | 只支持堂食与打包自取，不涉及配送、骑手、配送费 |
| 多语言 | 仅简体中文 |
| 顾客评价体系 | 数据库预留 `review` 表，功能 P2 |
| 小票云打印机对接 | 预留打印配置字段，功能 P2 |
| 库存精细管理 | 仅支持「不限量 / 每日限量 / 沽清」三态，不做原料级库存 |
| 排班、员工考勤 | 不涉及 |
| 连锁门店间数据互通 | 会员与积分按门店独立，不做跨店通用 |
| 营销活动（拼团、秒杀、加价购、套餐） | 本期不做 |
| 桌位预订 | 本期不做 |
| 支付对账、财务模块 | 本期不做 |

---

*本文档为 v1.0，如需求有变更请同步更新本文档及 `02-数据库设计.sql`、`03-API接口清单.md`。*
