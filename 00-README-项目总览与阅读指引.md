# 爱猫咖啡 · 扫码点餐系统 —— 项目文档包

> 本文档包用于交付给 AI 编程工具（如 Cursor / Claude Code / CodeBuddy 等）进行开发。
> 阅读顺序：本文件 → `01-需求规格说明书.md` → `04-视觉设计规范.md` → `02-数据库设计.sql` → `03-API接口清单.md`

---

## 一、文档包内容

| 文件 | 说明 |
| --- | --- |
| `00-README-项目总览与阅读指引.md` | 项目总览、技术栈、目录结构建议、全局技术约定、待确认事项 |
| `01-需求规格说明书.md` | 角色权限、功能需求（顾客端 / 店主端 / 超管后台）、业务流程、订单状态机、页面清单、视觉规范摘要、非功能需求 |
| `02-数据库设计.sql` | MySQL 8.0 建表语句，28 张表，含全部字段中文注释与索引，可直接执行 |
| `03-API接口清单.md` | 三端 REST API 清单，含路径、方法、入参、出参、权限说明 |
| `04-视觉设计规范.md` | **视觉风格（参考 HelloKitty 的粉白可爱风）+ IP 合规红线 + 色彩/字体/圆角/阴影设计令牌 + 组件规范 + 三端风格差异化 + uni.scss 变量** |

---

## 二、项目概述

**项目名称**：爱猫咖啡（Cat Coffee）扫码点餐系统

**业务场景**：一家以猫为主题的咖啡店，顾客到店后扫描桌贴二维码进入小程序点单，可选择**堂食**（需绑定桌号）或**打包**（自取，生成取餐码）。下单后订单实时推送至店主端，店主接单并制作，顾客到柜台**到店付款**。

**产品形态**（共 3 端）：

| 端 | 载体 | 使用者 | 技术栈 |
| --- | --- | --- | --- |
| 顾客端 | 微信小程序（同一小程序包内） | 到店顾客 | uni-app + Vue3 |
| 店主端 | 微信小程序（同一小程序包内，角色切换） | 门店店主 / 店员 | uni-app + Vue3 |
| 管理后台 | 独立 Web 站点 | 超级管理员 | Node.js 提供 API；前端建议 Vue3 + Element Plus |

> ⚠️ 顾客端与店主端**在同一个微信小程序内**，通过「角色切换 + 独立分包」实现，不发布两个小程序。

---

## 三、技术栈与技术选型

### 3.1 小程序端（顾客端 + 店主端）

| 项 | 选型 | 说明 |
| --- | --- | --- |
| 框架 | uni-app + Vue 3 | `<script setup>` 语法糖 |
| 语言 | TypeScript | 建议开启，便于维护 |
| 状态管理 | Pinia | 用户态、购物车、门店信息、商家态 |
| UI 组件库 | uview-plus 或 uni-ui | 二选一，全局统一 |
| 请求库 | 自封装 `uni.request` Promise 封装 | 统一拦截 token、错误码、loading |
| 图表 | qiun-data-charts（uCharts） | 店主端数据统计 |
| 地图（可选） | uni.openLocation | 店铺地址导航 |
| 编译目标 | 微信小程序（mp-weixin） | 主目标；H5 可选兼容 |

### 3.2 服务端

| 项 | 选型 | 说明 |
| --- | --- | --- |
| 运行时 | Node.js 18+ | LTS |
| 框架 | NestJS（推荐）或 Koa2 | 需要分层清晰、易扩展时选 NestJS；追求轻量选 Koa2 + 分层目录 |
| 数据库 | MySQL 8.0 | utf8mb4 / utf8mb4_unicode_ci |
| ORM | TypeORM 或 Prisma | 与框架配套即可；也可用 mysql2 + 手写 SQL |
| 缓存 | Redis（可选但建议） | 存 token 黑名单、取餐码序号、新订单轮询缓存 |
| 鉴权 | JWT（jsonwebtoken） | 三端各自签发，payload 带 `type` 区分 |
| 参数校验 | class-validator / joi | 统一校验中间件 |
| 文件上传 | multer + 本地磁盘 / 腾讯云 COS | 建议抽象 `UploadService`，便于切换 |
| 定时任务 | node-cron | 每日统计、优惠券过期、取餐码重置 |

### 3.3 管理后台前端（未指定，建议方案）

| 项 | 选型 |
| --- | --- |
| 框架 | Vue 3 + Vite |
| UI | Element Plus |
| 状态 | Pinia |
| 请求 | Axios（统一拦截器） |
| 图表 | ECharts 5 |

> ❓ **待确认**：管理后台前端框架当前未指定。若你已有偏好（React + Ant Design、Vue + Element），请告知，接口层不受影响。

### 3.4 视觉风格与主题

**风格定位**：日系可爱风（Kawaii），**粉白配色 + 圆润造型**，整体气质参考 **HelloKitty**。

| 项 | 约定 |
| --- | --- |
| 品牌主色 | 主粉 `#FF8FB1`、深粉 `#E8618C`、浅粉底 `#FFE1EC`、页面背景 `#FFF6F9` |
| 点缀色 | 蝶结黄 `#FFDC7A`（浅，标签底 / 角标底）、婴儿蓝 `#A9D9EA`；黄底上必须用深色文字 |
| 品牌 IP | 自研猫咪形象 **「爱咪」**（白猫 + 猫耳猫爪 + 咖啡师元素） |
| 圆角基调 | 圆润无锐角：卡片 24rpx、按钮胶囊 999rpx |
| 阴影基调 | 粉色调阴影，禁止黑色阴影 |
| 主题实现 | 顾客端与店主端共用 `uni.scss` 设计令牌；店主端降低粉色占比；管理后台覆盖 Element Plus 主题变量 |
| 深色模式 | 本期不做，锁定浅色外观 |

> ⚠️ **IP 合规红线**：HelloKitty 是三丽鸥的注册 IP，**只参考风格，不得使用其形象、名称、Logo 或字体**。详见 `04-视觉设计规范.md` 第一节。

完整设计令牌（可直接复制到项目）、组件规范、页面级风格指引见 **`04-视觉设计规范.md`**。

---

## 四、全局技术约定（三端必须一致遵守）

### 4.1 统一响应格式

```json
{
  "code": 0,
  "msg": "success",
  "data": {},
  "timestamp": 1739520000000
}
```

- `code = 0` 表示成功，非 0 表示业务异常，HTTP 状态码统一返回 200（除网关级错误）。
- 分页数据统一结构：

```json
{
  "list": [],
  "total": 128,
  "page": 1,
  "pageSize": 10
}
```

### 4.2 错误码表

| code | 含义 | 处理建议 |
| --- | --- | --- |
| 0 | 成功 | - |
| 400 | 参数错误 | 提示 `msg` |
| 401 | 未登录 / Token 失效 | 清本地 token，跳登录 |
| 403 | 无权限（越权访问其他门店数据） | 提示无权限 |
| 404 | 资源不存在 | 提示 |
| 409 | 业务冲突（重复提交、状态已变更） | 提示 `msg` 并刷新 |
| 500 | 服务器内部错误 | 通用错误提示 |
| 1001 | 手机号未绑定 | 弹出手机号授权 |
| 1002 | 门店休息中，暂不可下单 | 提示 |
| 1003 | 菜品已下架 / 已售罄 | 提示并刷新菜单 |
| 1004 | 订单状态不允许当前操作 | 刷新订单 |
| 1005 | 优惠券不可用 | 提示并移除券 |
| 1006 | 积分不足 | 提示 |
| 1007 | 桌号无效 / 已停用 | 引导手动选桌 |
| 2001 | 店主账号被禁用 | 提示联系管理员 |

### 4.3 认证方式

| 端 | 认证方式 | Header |
| --- | --- | --- |
| 顾客端 | 微信 code2session → 自定义 JWT（7 天） | `Authorization: Bearer <token>` |
| 店主端 | 手机号 + 密码 → JWT（7 天） | 同上 |
| 超管后台 | 账号 + 密码 + 图形验证码 → JWT（2 小时） | 同上 |

JWT payload 约定：

```json
{
  "sub": 123,                 // 主体 ID（user_id / store_user_id / admin_id）
  "type": "CUSTOMER",         // CUSTOMER | MERCHANT | ADMIN
  "storeId": 5,               // 仅 MERCHANT 有，用于强制数据隔离
  "iat": 1739520000,
  "exp": 1740124800
}
```

**越权防护（硬性要求）**：所有店主端接口必须由中间件强制注入 `storeId`，禁止由前端传入 `storeId` 决定数据范围。

### 4.4 金额与时间约定

- 金额统一 `DECIMAL(10,2)`，服务端计算，**前端只做展示，不参与金额运算**。
- 时间统一 `YYYY-MM-DD HH:mm:ss`（北京时间，数据库存 `DATETIME`）。
- ID 统一 `BIGINT UNSIGNED` 自增；对外接口以字符串返回，避免 JS 精度丢失。
- 软删除统一使用 `deleted_at`（NULL 表示未删除），查询默认过滤。

### 4.5 命名规范

- 数据库：表名、字段名 `snake_case`；表名用单数（`order` 为 MySQL 保留字，实际表名用 `order_main`）。
- 接口路径：`kebab-case`，资源名用复数。
- 枚举值：统一大写下划线，如 `DINE_IN`、`TAKEAWAY`、`PENDING`。

---

## 五、微信相关前置条件（开工前必须确认）

| 项 | 要求 | 影响 |
| --- | --- | --- |
| 小程序主体 | **企业主体**（非个人） | 个人主体**无法**使用手机号一键授权 |
| 手机号快速验证组件 | `button open-type="getPhoneNumber"` | 需在企业主体小程序中开通，按次收费（有免费额度） |
| 小程序码生成 | `wxacode.getUnlimited` | 用于桌贴二维码，scene 最长 32 字符 |
| 服务器域名 | 需在微信公众平台配置 request 合法域名（HTTPS） | 开发期可勾选「不校验合法域名」 |
| AppID / AppSecret | 需提供 | 服务端 code2session 必需 |
| 微信支付 | **本期不接入**（到店付款） | 后续如需在线支付，本设计已预留扩展点 |

---

## 六、建议的代码仓库结构

```
cat-coffee/
├── server/                          # Node.js 服务端
│   ├── src/
│   │   ├── config/                  # 配置（数据库、jwt、微信、上传）
│   │   ├── common/                  # 统一响应、错误码、装饰器、过滤器、拦截器
│   │   ├── middleware/              # 鉴权、门店隔离、日志、异常捕获
│   │   ├── modules/
│   │   │   ├── customer/            # 顾客端模块（auth/store/menu/order/member/coupon）
│   │   │   ├── merchant/            # 店主端模块（dashboard/order/dish/category/table/coupon/member/store/stat）
│   │   │   ├── admin/               # 超管模块（store/store-user/dashboard/config/log）
│   │   │   └── shared/              # 共用服务：微信、上传、订单号、取餐码、价格计算引擎
│   │   ├── entity/ 或 prisma/       # 数据模型
│   │   └── utils/
│   ├── sql/schema.sql
│   └── .env.example
├── miniapp/                         # uni-app 小程序
│   ├── src/
│   │   ├── pages/                   # 顾客端主包页面
│   │   ├── subpackages/
│   │   │   └── merchant/            # 店主端分包（体积隔离）
│   │   ├── components/
│   │   ├── store/                   # Pinia
│   │   ├── api/                     # 接口定义，按端分目录
│   │   ├── utils/
│   │   ├── static/
│   │   ├── App.vue
│   │   ├── pages.json
│   │   └── manifest.json
│   └── package.json
└── admin-web/                       # 管理后台前端
    └── src/
        ├── views/
        ├── api/
        ├── router/
        └── store/
```

**门店隔离原则**：`server/src/modules/merchant` 下所有 Service 方法的第一个参数必须是 `storeId`，由中间件注入，任何查询都要带 `store_id` 条件。

---

## 七、开发优先级建议（P0 / P1 / P2）

| 优先级 | 范围 | 说明 |
| --- | --- | --- |
| **P0 必须交付** | 微信授权登录、扫码点餐、菜单浏览、购物车、下单（堂食/打包）、订单状态流转、店主接单/制作/出餐、改单、分类与菜品管理（含规格）、超管门店与店主账号管理、店铺基本信息与营业状态 | 缺任意一项产品无法闭环 |
| **P1 首版建议含** | 会员等级 + 积分 + 优惠券、桌位管理与二维码生成、数据统计看板、操作日志、订单筛选与导出 | 核心经营能力 |
| **P2 后续迭代** | 菜品加料、评价体系、小票打印、WebSocket 实时推送、多店员权限细分、跨门店会员通 | 增强体验 |

---

## 八、待确认事项（开工前请逐条确认）

| # | 事项 | 当前默认处理 |
| --- | --- | --- |
| 1 | 管理后台前端技术栈 | 默认 Vue3 + Element Plus |
| 2 | 服务端框架 | 默认 NestJS（若偏好 Koa/Express 请说明） |
| 3 | 图片存储方案 | 默认本地磁盘 + 静态目录；建议生产环境切腾讯云 COS |
| 4 | 一个门店是否允许多个店主账号（多店员） | 默认**允许**，`store_user` 支持 `OWNER` / `STAFF` 两种角色 |
| 5 | 一个手机号是否可管理多个门店 | 默认**不可**（手机号全局唯一）。如需支持，改造为 `store_user` 与 `store` 多对多关联表 |
| 6 | 会员数据是否跨门店通用 | 默认**按门店独立**（`user_member` 带 `store_id`） |
| 7 | 是否需要小票打印机对接 | 默认 P2，预留打印配置字段与接口占位 |
| 8 | 是否需要顾客端评价 | 默认 P2，数据库表已预留 `review` |
| 9 | 是否需要开启微信支付 | 本期不接入，数据结构已预留 `pay_status` / 支付流水扩展位 |
| 10 | 优惠券过期时间是否精确到时分秒 | 固定日期券默认 `23:59:59` 过期 |
| 11 | 视觉方向是否需要在粉白基础上做调整 | 默认**日系粉白可爱风**，主色 `#FF8FB1`，详见 `04-视觉设计规范.md` |
| 12 | 品牌 IP 是否采用自研猫咪「爱咪」 | 默认采用；名称可改，但造型必须与 HelloKitty 明确区分（合规红线） |
| 13 | 插画、图标、字体素材由谁提供 | 默认需方提供设计稿与授权素材；开发期先用占位图与系统字体，不接入未授权素材 |
| 14 | 是否需要为猫咪 IP 申请著作权登记与商标 | 默认建议申请，属于长期品牌资产，不阻塞开发 |

---

## 九、交付验收标准

1. 顾客可用微信一键授权手机号完成登录，未授权时可浏览菜单但不可下单。
2. 扫码进入后自动带出桌号（堂食），打包订单无需桌号并生成当日唯一取餐码。
3. 下单后店主端 **10 秒内**（轮询间隔 5s）收到新订单提醒。
4. 订单状态严格按状态机流转，非法流转被服务端拒绝（返回 1004）。
5. 价格计算（商品合计 − 会员折扣 − 优惠券 − 积分抵扣）前后端结果一致，以服务端为准。
6. 店主改单后顾客端订单详情可见改单记录与最新金额。
7. 店主**无法**通过任何接口读取或修改其他门店数据（越权测试必须通过）。
8. 超管可创建门店 → 创建店主账号 → 店主登录后即可配置菜单并接单，全流程无需人工改库。
9. **视觉风格**：顾客端、店主端、管理后台三端视觉统一遵循 `04-视觉设计规范.md`；色值、圆角、阴影、间距全部引用设计令牌，无硬编码色值；店铺状态色语义三端一致。
10. **IP 合规**：项目产物中不存在任何 HelloKitty / 三丽鸥相关素材、名称或 Logo；所有插画与图标均有来源记录与商用授权凭证。
11. **三端风格差异化已落实**：顾客端可爱氛围到位；店主端与管理后台粉色占比受控（店主端 ≤ 15%），无装饰性插画与动效干扰操作。
12. **可读性达标**：粉色背景上文字对比度满足大字号 ≥ 3:1、正文 ≥ 4.5:1；不存在浅粉底白字或主粉写正文的情况。
